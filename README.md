# cc-attack
DDoS attacks targeting websites

usage
Install dependencies: Run pip install PySocks.


Prepare the proxy: Create a file named socks5.txt in the same directory and store the proxy in the format of IP:port.


To run the test: Start the script by entering the full path to the target (e.g., https://example.com/).
